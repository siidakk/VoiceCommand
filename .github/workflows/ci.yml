# Runs the test suite on every push and pull request.
#
# There is no install step: the project has no dependencies, so `npm ci` would
# have nothing to do. The matrix covers the oldest runtime the project claims
# to support alongside current LTS.
name: tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node: ['20', '22']

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}

      - name: Run tests
        run: npm test
